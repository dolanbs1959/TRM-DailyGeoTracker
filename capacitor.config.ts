import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'AngularBuild1',
  webDir: 'docs',
  server: {
    androidScheme: 'https'
  }
};

export default config;
